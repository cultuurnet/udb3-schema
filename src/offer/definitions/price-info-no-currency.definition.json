{
  "required": [
    "category",
    "name",
    "price"
  ],
  "properties": {
    "category": {
      "type": "string",
      "enum": [
        "base",
        "tariff"
      ]
    },
    "name": {
      "$ref": "translatable-string.definition.json"
    },
    "price": {
      "type": "number",
      "format": "float",
      "example": 10.0
    }
  }
}
