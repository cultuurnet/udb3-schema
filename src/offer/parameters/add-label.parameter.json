{
  "name": "body",
  "in": "body",
  "description": "An object containing the label to attach to an offer.",
  "required": true,
  "schema": {
    "$ref": "../definitions/label.definition.json"
  }
}