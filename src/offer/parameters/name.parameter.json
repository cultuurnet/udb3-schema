{
  "name": "body",
  "in": "body",
  "description": "An object containing the name in the given language.",
  "required": true,
  "schema": {
    "$ref": "../definitions/offer-name.definition.json"
  }
}