{
  "name": "country",
  "in": "query",
  "description": "Limit the list to places in the given country. This is optional. When no country parameter is provided, default 'BE' will be used.",
  "required": false,
  "default": "BE",
  "type": "string"
}